//there is a diffrence b/w let,var and const only  at block scope 

//at block scope if it is inside the block then it is treated as outside the block

// so if outside the block there is function then it is treatd as local variable  or if outside is not any function then it is treated as global variable